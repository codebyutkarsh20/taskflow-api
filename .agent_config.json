{
  "test_command": "pytest tests/ -v --tb=short",
  "test_timeout": 120,
  "setup_commands": [
    "pip install -r requirements.txt -q"
  ],
  "env": {
    "PYTHONPATH": "."
  },
  "max_explore_calls": 40,
  "min_confidence": 0.6,
  "skip_categories": [],
  "description": "TaskFlow API — simple task management FastAPI service with seeded bugs for agent testing"
}
